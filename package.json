{
  "name": "jezzball",
  "version": "0.1.0",
  "private": true,
  "description": "Multiplayer JezzBall — carve the arena, trap the atoms, bring your friends",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "check": "eslint . && node --test test/*.test.mjs && node test/headless-play.mjs && node test/smoke/client.mjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "jsdom": "^24.0.0",
    "socket.io-client": "^4.8.1"
  }
}
